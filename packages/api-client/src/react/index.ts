/**
 * Shared React hooks for @repo/api-client.
 *
 * Prefer generated query options from `@repo/api-client/query` in apps.
 * Add hooks here only when they provide shared domain behavior
 * (invalidation, staleTime, composition, etc.).
 */

export {}
