{
  "name": "@repo/auth",
  "version": "0.0.0",
  "private": true,
  "exports": {
    ".": "./src/index.ts",
    "./auth": "./src/auth.ts",
    "./authz": "./src/authz.ts",
    "./keys": "./src/keys.ts",
    "./tenant": "./src/tenant.ts"
  },
  "scripts": {
    "build": "tsc --project tsconfig.json --noEmit false --emitDeclarationOnly --declaration --outDir dist",
    "lint": "eslint .",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "test": "pnpm run test:unit",
    "test:unit": "vitest run",
    "boundaries": "eslint . --max-warnings=0"
  },
  "dependencies": {
    "@repo/database": "workspace:*",
    "@repo/salon-core": "workspace:*",
    "bcryptjs": "^3.0.3",
    "jose": "^6.2.2",
    "next": "16.2.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^22"
  }
}
