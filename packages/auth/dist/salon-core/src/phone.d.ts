/** Normalize phone for storage and unique lookup (digits only, Persian/Arabic → Latin). */
export declare function normalizePhone(input: string): string;
export declare function displayPhone(normalized: string): string;
